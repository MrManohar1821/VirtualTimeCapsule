// config.js
// By leaving this as empty, everything will go through the Vite proxy (defined in vite.config.js)
// This solves CORS issues and makes it easy to change the backend link in one place (.env file)
export const API_BASE_URL = "";
